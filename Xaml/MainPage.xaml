<Page
    x:Class="Grapher.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Grapher"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:xaml="using:Grapher.Xaml"
    mc:Ignorable="d">
    
    <Page.Resources>
        <local:GraphControls x:Key="Graphs"/>
    </Page.Resources>
    
    <Hub
        AllowDrop="True" 
        DragOver="Grid_DragOver" 
        Drop="Grid_Drop"
        Margin="20">

        <HubSection Header="Getting Started">
            <DataTemplate>
                <TextBlock 
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontSize="20">
                        Drop a file containing a statemachine here
                    </TextBlock>
            </DataTemplate>
        </HubSection>
        <HubSection>
            <DataTemplate>
                <ListView ItemsSource="{StaticResource Graphs}">
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"></StackPanel>
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                </ListView>
            </DataTemplate>
        </HubSection>

    </Hub>
    
</Page>
